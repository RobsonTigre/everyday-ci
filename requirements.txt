# Python dependencies for the Everyday Causal Inference code examples
# Tested with Python 3.9+. Install with: pip install -r requirements.txt
# Note: pycausalarima (chapter 11 only) needs Python 3.10+; pip skips it on older versions.
numpy
pandas<3  # pycausalimpact (chapter 11) uses APIs removed in pandas 3
scipy
matplotlib
seaborn
statsmodels
scikit-learn
linearmodels
diff-diff
econml
mcf  # only mcf 0.10.0+ needs Python 3.12+; pip picks an older compatible release below that
rdrobust
rddensity
pycausalimpact
pycausalarima ; python_version >= "3.10"
